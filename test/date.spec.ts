import { getDayNumber } from 'src/utils/date'

describe('Utils - date', () => {
  it('true', () => {
    expect(true).toEqual(true);
  });
  it('false', () => {
    expect(false).toEqual(false);
  });
})
